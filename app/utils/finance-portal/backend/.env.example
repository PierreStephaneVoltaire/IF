# AWS Configuration (credentials are picked up from instance IAM role)
AWS_REGION=ca-central-1

# DynamoDB
IF_FINANCE_TABLE_NAME=if-finance
IF_OPERATOR_PK=operator

# Server
PORT=3002
NODE_ENV=development
